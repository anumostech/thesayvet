version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: thesayvet_app
    restart: unless-stopped
    volumes:
      - .:/var/www/html
      - ./php/php.ini:/usr/local/etc/php/php.ini
    environment:
      APP_ENV: local
      APP_DEBUG: "true"
      DB_CONNECTION: mysql
      DB_HOST: db
      DB_PORT: 3306
      DB_DATABASE: thesayvet
      DB_USERNAME: thesayvetuser
      DB_PASSWORD: password
    depends_on:
      - db
    networks:
      - thesayvet_network

  db:
    image: mysql:8.0
    container_name: thesayvet_db
    restart: unless-stopped
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: thesayvet
      MYSQL_USER: thesayvetuser
      MYSQL_PASSWORD: password
    volumes:
      - thesayvet_dbdata:/var/lib/mysql
    networks:
      - thesayvet_network

  web:
    image: nginx:alpine
    container_name: thesayvet_nginx
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - .:/var/www/html
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - app
    networks:
      - thesayvet_network

  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: thesayvet_phpmyadmin
    restart: unless-stopped
    ports:
      - "8081:80"
    environment:
      PMA_HOST: db
      PMA_PORT: 3306
      MYSQL_ROOT_PASSWORD: root
    depends_on:
      - db
    networks:
      - thesayvet_network

volumes:
  thesayvet_dbdata:

networks:
  thesayvet_network:
    driver: bridge
